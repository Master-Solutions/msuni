import React from 'react'
import styled from '@emotion/styled';
import { flexbox } from 'styled-system';
import Box from '../Box/Box';

export const FlexBox = styled(Box)({display: 'inline-flex'}, flexbox);

export default FlexBox;
